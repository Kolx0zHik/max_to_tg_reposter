services:
  bridge:
    image: ghcr.io/kolx0zhik/max_to_tg_reposter:latest
    env_file: .env
    restart: unless-stopped
    volumes:
      - ./config:/app/config:ro
      - ./data:/app/data
      - ./max_session:/app/.max_session
